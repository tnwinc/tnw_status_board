<header>
  <h4>{{date start}} - {{date finish}}</h4>
  {{#if hasStories}}
    <a href="#" class="toggle-expansion" {{action 'toggleExpansion'}}>
      {{expando_icon expanded}}
    </a>
  {{/if}}
</header>

<ul class="stories">
  {{#each filteredStories}}
    {{partial 'story'}}
  {{/each}}
</ul>
