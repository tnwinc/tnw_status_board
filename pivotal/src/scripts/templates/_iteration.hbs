<article {{bind-attr class="expanded :iteration"}}>
  <header>
    <h4>{{date start}} - {{date finish}}</h4>
    {{#if hasStories}}
      <a href="#" class="toggle-expansion" {{action 'toggleExpansion' this}}>
        {{expando_icon expanded}}
      </a>
    {{/if}}
  </header>
  <ul class="stories">
    {{#each stories}}
      {{partial 'story'}}
    {{/each}}
  </ul>
</article>
